# Backend Source

서버 및 데이터 처리 담

## 파일 및 디렉토리 역할

| 파일        | 역할                            |
| -------------- | ----------------------------- |
| `app.js`       | Express 앱의 기본 설정을 담당          |
| `server.js`    | 서버를 실제로 실행                    |

| 디렉토리           | 역할                            |
|----------------| ----------------------------- |
| `routes/`      | API 주소를 정의                    |
| `controllers/` | 요청을 받고 응답을 반환                 |
| `services/`    | 검색, 필터링, 시간표 충돌 확인 등 핵심 로직 처리 |
| `models/`      | 강의, 사용자 등 데이터 구조 정의           |
| `db/`          | 데이터베이스 연결 및 초기 설정 관리          |
| `data/`        | DB 연결 전 사용할 임시 JSON 데이터 저장    |

